worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

events {
  worker_connections  1024;  ## Default: 1024
}
http {
    server {
    listen 80;
    listen [::]:80;

    server_name _;
    rewrite ^ http://10.1.2.x$request_uri permanent;
    }
}